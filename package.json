{
  "name": "mware-middleware",
  "version": "1.0.0",
  "description": "ECPOS backend middleware for Vercel",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build complete'"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "engines": {
    "node": "18.x"
  }
}